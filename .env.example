# Copy to .env.local for local overrides (never commit .env.local).
# Production URL on Vercel can match app/lib/site.ts or override via env later.

# NEXT_PUBLIC_SITE_URL=https://www.glasbazaporoko.si
