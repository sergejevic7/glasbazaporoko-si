import { Violin } from "./Icons";

/** Skladno z ikono pri kartici »Brezhibna izvedba« — violina z zasukom 25° (definicija v `Icons.tsx`). */
export function BrandMarkIcon({ className }: { className?: string }) {
  return <Violin className={className} />;
}
